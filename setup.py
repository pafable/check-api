import setuptools

setuptools.setup(
    name='check-api',
    version='0.0.1',
    description="phil's test project",
    url='https://github.com/pafable/check-api',
    author='pafable',
    packages=setuptools.find_packages()
)